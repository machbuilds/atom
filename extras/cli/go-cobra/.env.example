# Go CLI — environment contract.
#
# CLIs typically don't need much env config; flags + config files cover
# most cases. Add entries here only for runtime knobs that don't belong
# on the command line.

# Example: enable verbose logging for debug builds
# APP_LOG_LEVEL=debug
