# TypeScript library — environment contract.
#
# Libraries usually don't read env vars at runtime — that's the
# consumer's concern. Add entries here only if your library has
# build-time toggles or test-only knobs.

# Example: skip integration tests that hit external services
# SKIP_INTEGRATION=1
