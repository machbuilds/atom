# {{PROJECT_NAME}}

{{DESCRIPTION}}

{{QUICK_START}}

## Project notes

This project was scaffolded from [atom](https://github.com/machbuilds/atom).
The `AGENTS.md` file at the root is the canonical instruction set every
AI tool reads — Claude, Codex CLI, Gemini, Cursor, GitHub Copilot.
Edit it as the project evolves.

Cross-project memory and a personal playbook live in `~/.atom/`:

- `~/.atom/nucleus/projects/{{PROJECT_NAME}}/` — session notes, low bar
  to capture
- `~/.atom/learnings/` — graduated patterns that follow you into every
  new project

If you haven't yet, run once per machine:

```bash
nucleus init
learnings init
```
